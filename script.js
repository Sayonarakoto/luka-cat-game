var score=0
const lukaBox = luka.getBoundingClientRect();
const fishBox = fish.getBoundingClientRect();

 function lukacollectfish(){
    while(lukaBox>fishBox){
        const lukaEl = document.querySelector('.luka');
const fishEls = Array.from(document.querySelectorAll('.fish'));
        lukaEl=setInterval(30,60)


    }
 }